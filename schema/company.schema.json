{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/evilmartians/devtools-pmf/schema/company.schema.json",
  "title": "DevTools PMF company file",
  "type": "object",
  "additionalProperties": false,
  "required": ["company", "slug", "url", "sub_industry", "gtm", "metrics"],
  "properties": {
    "company": { "type": "string", "minLength": 1 },
    "slug": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
    "url": { "type": "string", "minLength": 1 },
    "sub_industry": { "type": "string", "enum": ["tooling", "infra", "security"] },
    "gtm": { "type": "string", "enum": ["PLG", "Sales-assisted", "Enterprise"] },
    "verified_at": { "type": "string" },
    "metrics": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/metric" }
    },
    "plays": {
      "type": "array",
      "items": { "$ref": "#/$defs/play" }
    }
  },
  "$defs": {
    "play": {
      "type": "object",
      "additionalProperties": false,
      "required": ["title", "summary", "goal", "learning", "when_it_works", "tags", "tier", "source_type"],
      "properties": {
        "title": { "type": "string", "minLength": 1 },
        "summary": { "type": "string", "minLength": 1 },
        "goal": { "type": "string", "enum": ["Acquisition", "Activation", "Conversion", "Retention", "Expansion", "Revenue"] },
        "learning": { "type": "string", "minLength": 1 },
        "when_it_works": { "type": "string", "minLength": 1 },
        "metric_moved": { "type": "string" },
        "result": { "type": "string" },
        "tags": { "type": "array", "minItems": 1, "items": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" } },
        "tier": { "type": "string", "enum": ["Verified", "Reported", "Estimated"] },
        "source_type": {
          "type": "string",
          "enum": ["filing", "press", "spokesperson", "journalism", "aggregator", "estimate"]
        },
        "source_url": { "type": "string" },
        "attribution": { "type": "string" }
      },
      "allOf": [
        {
          "comment": "Verified and Reported plays must carry a real source URL.",
          "if": { "properties": { "tier": { "enum": ["Verified", "Reported"] } } },
          "then": {
            "required": ["source_url"],
            "properties": { "source_url": { "type": "string", "format": "uri", "minLength": 1 } }
          }
        }
      ]
    },
    "metric": {
      "type": "object",
      "additionalProperties": false,
      "required": ["field", "value", "tier", "source_type"],
      "properties": {
        "field": {
          "type": "string",
          "enum": ["arr", "funding_round", "ttfv", "retention", "nrr", "free_to_paid", "organic"]
        },
        "value": { "type": "string", "minLength": 1 },
        "as_of": { "type": "string" },
        "tier": { "type": "string", "enum": ["Verified", "Reported", "Estimated"] },
        "source_type": {
          "type": "string",
          "enum": ["filing", "press", "spokesperson", "journalism", "aggregator", "estimate"]
        },
        "source_url": { "type": "string" },
        "attribution": { "type": "string" },
        "note": { "type": "string" },
        "moved_the_needle": { "type": "string" },
        "tags": { "type": "array", "items": { "type": "string" } }
      },
      "allOf": [
        {
          "comment": "Verified and Reported data points must carry a real source URL.",
          "if": { "properties": { "tier": { "enum": ["Verified", "Reported"] } } },
          "then": {
            "required": ["source_url"],
            "properties": { "source_url": { "type": "string", "format": "uri", "minLength": 1 } }
          }
        }
      ]
    }
  }
}
